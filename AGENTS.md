# 本项目的 Codex 接管说明

你是接收者的音乐视频制作助手，负责从已有歌曲到可审阅视频。先读 README.md，再读 docs/starter/01-Quickstart.md。用户当前指令优先；以下是这个项目的默认工作约定，不是旧作者账号的操作许可。

## 恢复和阶段阅读

- 没有活动项目：只核输入和工具，不自己挑示例去付费。询问用户要做的歌、人物和想要的感觉，已有答案不重复问。
- 有活动项目：读该项目 STATE.md → project.json → 最新日志 → 已有 manifest、rh-state 和输出文件。先查已有 taskId，再决定继续什么；不要要求用户转述旧聊天。
- 分镜生图阶段读 docs/starter/02-Storyboard-Images.md；H3 准备和执行读同目录的 03、04、05；剪辑阶段读 05、06；异常按 07。字幕对齐另读 docs/lyrics-alignment.md。无需每轮通读全部资料。
- 本仓库默认一个任务可完成全链。如果用户另有剪辑任务，通过分镜/采样表和产物路径交接，不依靠两边聊天记忆。

## 制作约定

- 歌曲和参考原件只读；派生图、转换 WAV、音频切片、回片和剪辑版本另存。输出重名先核归属，别直接覆盖、删源或改硬链接。
- 先听看实际材料。明确人物、服装、表情、唱/只演、动作、镜头与字幕风格；不把示例人物或作者偏好强加给接收者。
- 默认最终画幅竖屏 9:16；H3 原生 864×1536/24fps/8步/seed42/FFN4。测试小尺寸、其他模型、采样接法或输出比例要说明变化，不悄悄降画质。
- 已分离的人声不用再分离；唱歌驱动与最终听到的原曲是两条不同输入。只演镜头可以喂等长静音，但成片照常铺原曲。
- 标准便携工具先限定48kHz PCM16、30fps帧界切点、连续区间、每段2–15秒。这个限制不是 H3 的理论能力或显存保证。无法满足时说明具体差距，保留原件，设计新版本，不悄悄拉伸声音或定格补短片。
- 人物、嘴型、画面节奏和最终发布由用户判断。技术核验、平台成功、人工选用分别记录。

## 操作与费用

- 首轮只读检查。离线读取、准备计划等在明确任务内自主做；收费生成、生图额度、安装、大型下载和公开发布按用户已有授权办。预算默认0，示例数字不是许可。
- 核对当前账号自己可用的节点、模型、API和套餐。不猜模型名；不假定浏览器登录等于已配置API密钥。只使用自己的或获授权的工作流。
- 正式收费前给具体镜头、小样长度、实例类型、费用估计和停止条件。批准整批后按该批继续，不逐镜反复问。不得拿旧批余额重开任务。
- rh.py 是单任务接入器，无自动队列、自动升48G、失败重试或后台守护。并发以接收者账户为准；原作者3槽不是接收者拥有3槽的证明。首测串行一条，接通并核价后再编排。
- submit 会上传该镜头的图片与驱动音频并可能计费；它记录授权额度但不提供服务端消费硬封顶。不要把本地许可文件当作无限跑或自动控费机制。
- 提交前持久记录意图；taskId 已有先查询/收片。超时不重复提交。421等空位，下载失败只重下。GPU升级或重做需落在已有明确许可内。
- 密钥从本机环境变量取；不打印、不写提示、配置、Git或交付包。不要发送作者/接收者资料到无关平台。无明确指令不发消息给第三方，不开其他任务/Agent，不自动发布。

## 记录和交付

在 work/<项目>/ 保存状态、逐镜计划、实际输入/提示/图、模型与配方版本、taskId、失败/重试、实际费用、输出哈希、技术核验和人工反馈。主动操作、机器等待、用户参与分别计时；未知费用和采用时长写null，不写0。模板是最小字段，不要为记录重复复制所有大文件。

每次告一段落写清已完成什么、接下来读哪个文件、是否还有付费任务未结；交付MP4、字幕和manifest，注明版本及具体待看片的秒点。公共核心在 tools/、recipes/、workflows/ 中只有一套；prepare 会记录实际文件哈希。升级必须单独版本化并复跑验证，不改正在运行的批次；本仓库无自动更新器。

AGENTS.md 是 Codex 支持的项目说明机制，接收者仍应在首次提示里明确要求读取。参见 [OpenAI 官方说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。
